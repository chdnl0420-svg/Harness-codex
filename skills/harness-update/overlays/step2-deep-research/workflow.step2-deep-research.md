## CRITICAL: Step 2 Deep Research Contract

Step 2 is:

`한 줄 목표 → 6 카테고리 합리적 가정 또는 사용자 답변 → 필요 시 harness-deep-researcher 외부 리서치 → Open Questions → domain-<slug>.html`

Older summaries that omit the research branch are incomplete. The caller Codex must check the Step 2 research triggers before Codex review and before writing the final domain artifact.

If Step 2 research runs, the progress file must record:

- research topic
- tier
- adapter used: `harness-deep-researcher` skill, helper/sub-agent, or caller Codex direct
- saved research artifact path
- which findings were included in the domain plan

If Step 2 research does not run, the progress file must record the reason.
